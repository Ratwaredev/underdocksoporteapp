{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main and admin windows.",
  "windows": ["main", "admin"],
  "permissions": ["core:default", "opener:default", "updater:default"]
}
